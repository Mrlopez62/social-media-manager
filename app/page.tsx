export default function HomePage() {
  return (
    <main style={{ maxWidth: 840, margin: "48px auto", fontFamily: "sans-serif", lineHeight: 1.5 }}>
      <h1>One-Stop Social Publisher</h1>
      <p>MVP scaffold is ready for auth, connections, composer, scheduling, and publishing APIs.</p>
      <p>Check <code>/docs</code> and <code>/supabase/migrations</code> for architecture and schema baselines.</p>
    </main>
  );
}
